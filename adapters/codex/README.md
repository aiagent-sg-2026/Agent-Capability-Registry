# Codex local adapter

`codex-local-v1` installs a verified capability under a caller-selected target directory, with no broad permissions. The adapter copies declared files only and records an install receipt. Codex integration should load the installed `skills/` and metadata according to the target workspace policy; V1 does not modify global configuration.
