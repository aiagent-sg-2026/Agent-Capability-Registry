# PostgreSQL query review

Input: SQL plus optional schema/context. Return bounded findings and review guidance. Treat high-risk write-without-where as requiring human confirmation. PostgreSQL 16, 17, and 18 syntax is accepted as text; no server is required.
