# Progress

- Completed and verified: strict schema-driven manifest validation with recursive additional-property rejection; package file allowlisting with traversal, symlink, and non-regular-file rejection; evaluator runtime-artifact hashing; evaluator mismatch diagnostics and UNKNOWN handling for malformed evidence; bounded SQL heuristics with false-positive controls; gate-injected factory stop-on-failure behavior; version-preserving, collision-safe, idempotent installation receipts.
- Completed verification: `npm run typecheck`, `npm run build`, `npm test` (39 passing tests), example validate/verify, install smoke/idempotency, and adversarial hook/extra-file/traversal/symlink CLI exits (all nonzero), including validation from a foreign cwd. Integrity binds `capability.json` and all declared content files.
- Deferred: remote registry, signing/attestation, full SQL parser, endpoint integration, and persistent upgrade history/rollback UI. SQL review remains explicitly heuristic and does not claim full parsing.
- Package Factory repaired and live-verified after the independent host audit. Pre-repair, `factory once` returned HTTP 400 before any usage-ledger entry because the chat payload included internal `purpose`; repo state was unchanged and no secret was persisted. After repair, the first full live attempt hit a transient HTTP 504 and correctly recorded zero tokens plus transient streak 1. Immediate bounded probes then returned HTTP 200 for both `demo-auto` and `demo-openai-mini` (72 tokens each), so `demo-auto` remains the default. A fresh full cycle then saved a `CANDIDATE_ONLY` PostgreSQL-review candidate: proposal 154 tokens (84 prompt + 70 completion), review 210 (124 + 86), 364 total, leaving 999,636 of the isolated local 1,000,000-token admission budget; git was unchanged, secret scan was clean, and the circuit reset to streak 0. The 1,000,000 value is a local admission budget, not a claim about gateway quota. Clean host revalidation then passed `npm ci`, typecheck, build, all 18 tests, npm audit with 0 vulnerabilities, `systemd-analyze verify`, and a static scan confirming the Factory has no child-process execution, git-push, npm-publish, or package-install primitive. Production systemd installation is still pending this commit; the shared `github-pages` project/origin registration remains temporary and a dedicated ACR registration is preferred.

- Continuous Package Factory V1 implemented in this clone: append-only bounded opportunity refill, per-purpose usage admission, strict package validation, deterministic materialization, candidate indexing, and safe status counts. No production deployment is claimed.
- Continuous Factory verification covers legacy `seedId` consumption, terminal queue outcomes, discovery refill/deduplication, strict candidate packages, atomic materialization, duplicate hashes, review/budget gates, and an offline multi-cycle simulation. No network calls, installation, publishing, generated execution, commit, or push were performed.
- Registry Search V1 adds read-only Factory candidate indexing plus deterministic `cap search` and exact `cap info`; malformed, mismatched, duplicate, non-candidate, missing, and path-escaping records are skipped fail-closed. Search returns metadata/path only and does not execute or install candidate content.

- Agent Resolution V1 adds deterministic `cap resolve` and bounded context-only `cap use`, completing search → info → select → use without executing or installing candidate content.

- Agent Runner Integration V1 adds `cap agent codex|pi <task>`: deterministic natural-language task reduction, Registry Search → info/select → bounded context-only use, low-trust user-context framing, shell-free runner spawning, read-only default, dry-run redaction, no-match fail-closed behavior, and exit-code propagation. Deterministic verification is 42/42 tests with npm audit 0; live smoke launched Codex 0.153.4 (gpt-5.6-sol, high, read-only) and Pi 0.85.1 (read-only), both exit 0. No Gateway, systemd, DNS, or deployment configuration changed.

- High-Value Capability Seeds V1 adds five practical curated opportunities: ERP read-first investigation, PostgreSQL report-query review, API contract review, webhook delivery debugging, and ERP data-lineage tracing. Curated seeds now reconcile append-only into non-empty long-running opportunity state without duplicate ids/slugs/titles or state rewrites.

- Factory Generation Retry V1 aligns the proposal prompt with the strict candidate validator contract and retries malformed_json, unsafe_candidate, and review_failed outcomes up to three bounded attempts before consumption; candidate_saved and duplicate remain immediately terminal.
\n- Capability Quality V1 adds a deterministic depth gate for future Factory output: concrete When to use / Evidence required / Procedure / Failure modes / Output contract / Guardrails sections, a bounded 0.7–2.2 KB README, and exactly three concise distinct eval cases. The model review now rejects generic prose; no larger repository/context payload is introduced.\n
- Automation Dashboard V1 adds a dependency-free GitHub Pages view plus a sanitized runtime-state exporter for Cost Ledger, bounded cycle outcomes, retries, capability catalog metadata, agent task flow, and trust boundaries. Pages deployment is automatic on main changes; live VM snapshot publication remains intentionally separate because Factory has no Git-push permission.
